# Pinned per step.md 0.2. Install the Tesseract binary separately —
# this is only the Python wrapper (apt install tesseract-ocr).
opencv-python-headless==5.0.0.93
pytesseract==0.3.13
google-genai==2.20.0
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.4
pytest==9.1.1
httpx==0.28.1
python-docx==1.2.0  # step 0's marks-grid-template.docx row-height fix, and
                      # step 3r.6a's collection-sheet generator

# The local CNN recognizer is the default path as of 2026-08-30 (step 3r.6e),
# so its *inference* dependencies belong here rather than in
# requirements-cnn.txt — without them the app cannot start with RECOGNIZER
# unset. Training deps (torch/torchvision/onnx) stay in requirements-cnn.txt:
# they are genuinely optional, and nothing under app/ imports them. Verified
# by poisoning `torch` in sys.modules and constructing CNNRecognizer clean.
onnxruntime==1.23.2  # runs cnn/checkpoints/digit_cnn.onnx
scipy==1.15.3        # cnn/preprocess.py's ndimage.center_of_mass. Load-bearing,
                     # not incidental: MNIST centres a glyph by centre of mass,
                     # not bounding-box centre, and matching that is worth
                     # several points of real accuracy (plan.md §16).
                     # (This said "cnn/segment.py's connected-component
                     # labelling" until 2026-08-31 — segment.py uses OpenCV's
                     # connectedComponentsWithStats and needs no scipy at all.)
